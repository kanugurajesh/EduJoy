/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['codehelp-portfolio-website.netlify.app','drive.google.com','ik.imagekit.io']
    }
}

module.exports = nextConfig
